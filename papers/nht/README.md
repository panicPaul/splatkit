# Neural Harmonic Textures

Paper-style marimo notebook for Neural Harmonic Textures on Ember's
NHT-family backend.

The notebook keeps the method-specific shader, initialization, optimizer
configuration, and loss in the paper artifact. The backend named `nht.3dgut`
provides upstream-style 3DGUT feature accumulation for the deferred shader.
