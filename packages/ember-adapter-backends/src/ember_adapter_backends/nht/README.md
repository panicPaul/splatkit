# `adapter.nht`

Reference adapter for Neural Harmonic Textures.

This backend wraps the NHT-capable `gsplat` fork pinned by
`third_party/neural-harmonic-textures/gsplat`. It is intended as the reference
oracle for `ember-native-nht`, not as the production backend.
